# Legacy Tkinter client

`horsecare_gui.py` is preserved only as evidence of the original desktop design.
It is not the main frontend and is not used by the distributed web application.
The supported frontend is the Lovable/TanStack web project in `frontend/`.
